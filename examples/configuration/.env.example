# HashId Bundle v4 Environment Variables Example
# Copy this file to .env.local and set your values

# ============================================
# BASIC CONFIGURATION
# ============================================

# Default hasher salt (required)
# Generate a unique salt for your application
HASHID_SALT=your-unique-salt-here-change-this

# ============================================
# MULTIPLE HASHERS CONFIGURATION
# ============================================

# Secure hasher for sensitive resources (users, payments, etc.)
# Use a different, stronger salt for enhanced security
HASHID_SECURE_SALT=very-secure-salt-for-sensitive-data

# Public hasher for non-sensitive, publicly shared content
# Can use a simpler salt since these are public resources
HASHID_PUBLIC_SALT=public-salt-2024

# API hasher for external API resources
# Keep consistent for API backward compatibility
HASHID_API_SALT=api-salt-for-external-services

# Admin hasher for administrative resources
# Extra security for admin-only resources
HASHID_ADMIN_SALT=super-secret-admin-salt

# ============================================
# TYPED ENVIRONMENT VARIABLES
# ============================================

# You can also configure numeric values via environment
# HASHID_MIN_LENGTH=10
# HASHID_SECURE_MIN_LENGTH=20
# HASHID_PUBLIC_MIN_LENGTH=5

# ============================================
# ADVANCED CONFIGURATION
# ============================================

# Custom alphabet (optional, defaults provided)
# HASHID_ALPHABET=abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890

# Secure hasher alphabet with special characters
# HASHID_SECURE_ALPHABET=abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ1234567890!@#$%^&*

# ============================================
# SALT GENERATION TIPS
# ============================================

# Generate secure salts using:
# 
# PHP:
#   php -r "echo bin2hex(random_bytes(32));"
#
# OpenSSL:
#   openssl rand -hex 32
#
# Symfony Console:
#   php bin/console security:hash-password --no-interaction
#
# Online (for development only):
#   https://www.random.org/strings/

# ============================================
# SECURITY RECOMMENDATIONS
# ============================================

# 1. Never commit real salt values to version control
# 2. Use different salts for different environments (dev/staging/prod)
# 3. Use longer, more complex salts for sensitive resources
# 4. Rotate salts periodically (requires URL migration strategy)
# 5. Store production salts in secure secret management systems
# 6. Use different salts for different hasher contexts

# ============================================
# DOCKER CONFIGURATION
# ============================================

# If using Docker, you can pass these as build args or runtime environment:
# docker run -e HASHID_SALT=your-salt-here your-app
# docker-compose.yml:
#   environment:
#     - HASHID_SALT=${HASHID_SALT}
#     - HASHID_SECURE_SALT=${HASHID_SECURE_SALT}